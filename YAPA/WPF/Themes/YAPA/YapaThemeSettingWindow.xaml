<UserControl x:Class="YAPA.WPF.Themes.YAPA.YapaThemeSettingWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YAPA.WPF.Themes.YAPA"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <TextBlock Text="Use white text" Grid.Column="0" Grid.Row="0"></TextBlock>
            <CheckBox IsChecked="{Binding UseWhiteText, Mode=TwoWay}"  Grid.Column="1" Grid.Row="0"></CheckBox>

            <TextBlock Text="Count backwards" Grid.Column="0" Grid.Row="1"></TextBlock>
            <CheckBox IsChecked="{Binding CountBackwards, Mode=TwoWay}" Grid.Column="1" Grid.Row="1"></CheckBox>

        </Grid>
    </Grid>
</UserControl>

