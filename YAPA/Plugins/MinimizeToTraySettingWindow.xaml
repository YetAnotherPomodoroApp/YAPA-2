<UserControl x:Class="YAPA.WPF.MinimizeToTraySettingWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YAPA.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <CheckBox IsChecked="{Binding MinimizeToTray, Mode=TwoWay}"  Grid.Column="0" Grid.Row="0" Margin="5,5,5,0"></CheckBox>
            <TextBlock Text="Minimize to tray" Grid.Column="1" Grid.Row="0" Margin="0,5"></TextBlock>

            <CheckBox IsChecked="{Binding ShowInTaskbar, Mode=TwoWay}" Grid.Column="0" Grid.Row="1" Margin="5,5,5,0"></CheckBox>
            <TextBlock Text="Show in taskbar" Grid.Column="1" Grid.Row="1"  Margin="0,5"></TextBlock>

        </Grid>
    </Grid>
</UserControl>
